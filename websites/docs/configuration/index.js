'use strict';

const processenv = require('processenv');

const news = require('./news.json'),
      versions = require('./versions-autogenerated.json');

const configuration = {
  baseUrl: processenv('BASE_URL') || 'https://docs.wolkenkit.io',
  news,
  title: processenv('TITLE') || 'wolkenkit Documentation',
  versions
};

module.exports = configuration;
